{
  "scripts": {
    "postinstall": "npm run client:build",
    "client:dev": "react-scripts --openssl-legacy-provider start",
    "client:build": "react-scripts --openssl-legacy-provider build",
    "server:dev": "nodemon index.js",
    "db:build": "node ./db/init_db",
    "start": "node index.js"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "morgan": "^1.10.0",
    "pg": "^8.8.0"
  }
}
